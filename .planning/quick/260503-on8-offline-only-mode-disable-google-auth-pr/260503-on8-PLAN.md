---
phase: quick-260503-on8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/DataContext.tsx
  - src/pages/Settings.tsx
  - src/pages/Dashboard.tsx
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can launch the app and reach onboarding without any Google sign-in"
    - "User can export their data to a JSON file from Settings"
    - "User can import data from a JSON file in Settings, replacing all local data"
    - "Timer continues running when the desktop window is hidden via tray close button"
    - "Timer resets when the desktop app is fully killed (process exit)"
    - "Settings page shows no Google auth, sync, or account sections"
  artifacts:
    - path: "src/App.tsx"
      provides: "Offline-only boot flow (no auth checks)"
      must_not_contain:
        - "supabase.auth.getSession"
        - "signInWithGoogle"
        - "AuthCallback"
        - "google-signin"
        - "name-prompt"
        - "sb-lpnjxkzeyiifzzycmftg-auth-token"
    - path: "src/DataContext.tsx"
      provides: "Data provider without sync scheduling"
      must_not_contain:
        - "scheduleSync"
        - "pushData"
        - "isSyncEnabled"
    - path: "src/pages/Settings.tsx"
      provides: "Settings with export/import buttons, no auth/sync sections"
      contains:
        - "exportDataToFile"
        - "importDataFromFile"
      must_not_contain:
        - "SyncSection"
        - "WebAccountSection"
        - "signInWithGoogle"
    - path: "src/pages/Dashboard.tsx"
      provides: "Timer not reset on Tauri window close-requested (beforeunload-only reset)"
      must_not_contain:
        - "onCloseRequested"
  key_links:
    - from: "src/pages/Settings.tsx"
      to: "src/storage/index.ts"
      via: "import of exportDataToFile, importDataFromFile"
      pattern: "import.*exportDataToFile.*from.*storage"
    - from: "src/pages/Dashboard.tsx"
      to: "localStorage (studyflow.timer)"
      via: "beforeunload event listener (not Tauri onCloseRequested)"
      pattern: "beforeunload"
---

<objective>
Offline-only mode: disable Google auth while preserving all source files, add JSON export/import buttons in Settings, and fix timer persistence on tray close.

Purpose: Make Kaizen fully usable offline without any Google authentication requirement. Users back up and restore data via JSON export/import. Timer state is not reset when the window is hidden to tray.
Output: Modified App.tsx, DataContext.tsx, Settings.tsx, Dashboard.tsx
</objective>

<execution_context>
</execution_context>

<context>
@src/App.tsx
@src/DataContext.tsx
@src/pages/Settings.tsx
@src/pages/Dashboard.tsx
@src/storage/index.ts
@src/sync/index.ts
@src/supabase.ts
@src/auth.ts
@CLAUDE.md

## Imports structure
- src/App.tsx currently imports: supabase from ./supabase, pullData/pushData/setSyncEnabled/signInWithGoogle from ./sync, loadData/saveData from ./storage, createEmptyData from ./storage/types
- src/DataContext.tsx currently imports: loadData/saveData from ./storage, isSyncEnabled/pushData from ./sync, debugSaveAppData from ./debug
- src/pages/Settings.tsx currently imports: supabase from ../supabase, AppData type, useData from DataContext, saveData from storage, and 10 sync functions
- src/pages/Dashboard.tsx currently has a useEffect with both beforeunload and Tauri onCloseRequested handlers that mark the timer as closed

## Existing functions in storage/index.ts
- exportDataToFile(data: AppData): Promise<void> -- saves to file using Tauri dialog or browser download
- importDataFromFile(): Promise<AppData | null> -- reads from file using Tauri dialog or browser file input
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Disable Google auth in App.tsx and DataContext.tsx (preserve all source files)</name>
  <read_first>
    - /home/margy/Documents/app-studio/src/App.tsx (the file being modified)
    - /home/margy/Documents/app-studio/src/DataContext.tsx (the file being modified)
    - /home/margy/Documents/app-studio/src/sync/index.ts (code preserved, not imported)
    - /home/margy/Documents/app-studio/src/supabase.ts (code preserved, not imported)
    - /home/margy/Documents/app-studio/src/storage/index.ts (exportDataToFile, importDataFromFile exist)
  </read_first>
  <files>src/App.tsx, src/DataContext.tsx</files>
  <action>
    **DataContext.tsx changes:**
    1. Remove `import { isSyncEnabled, pushData } from "./sync";` -- no longer needed
    2. Delete the `scheduleSync` function (lines 14-23) entirely
    3. In the `setData` callback, remove the `scheduleSync();` call (line 39). Keep `saveData(next);` and `debugSaveAppData(next).catch(() => {});`

    **App.tsx changes:**
    1. Remove the import line `import { supabase } from "./supabase";` (line 14)
    2. Remove the import line `import { pullData, pushData, setSyncEnabled, signInWithGoogle } from "./sync";` (line 15)
    3. Change the `savedData` assignment (line 68): remove the `isTauri` guard so both web and desktop use localStorage. Change from:
       `const savedData = typeof window !== "undefined" && isTauri ? loadData() : null;`
       to:
       `const savedData = typeof window !== "undefined" ? loadData() : null;`
    4. Update the `phase` state union type (line 75): remove `"google-signin"` and `"name-prompt"`. Change from:
       `useState<"splash" | "onboarding" | "google-signin" | "name-prompt" | "profile" | "tour" | "app">`
       to:
       `useState<"splash" | "onboarding" | "profile" | "tour" | "app">`
    5. Replace the entire initial phase function body (lines 76-111): remove ALL supabase auth token (`sb-lpnjxkzeyiifzzycmftg-auth-token`) checks. New logic:
       - Desktop with `studyflow.active === "false"` -> "splash"
       - Has savedData and tourComplete -> "app"
       - Has savedData and no tourComplete -> "tour"
       - No savedData -> "splash"
       Example structure:
       ```
       () => {
         if (isTauri && localStorage.getItem("studyflow.active") === "false") return "splash";
         if (savedData) {
           if (localStorage.getItem("studyflow.tourComplete") !== "true") return "tour";
           return "app";
         }
         return "splash";
       }
       ```
    6. Replace the entire `handleSplashDone` function (lines 165-241): remove ALL supabase.auth.getSession calls, pullData calls, pushData calls, setSyncEnabled calls. New logic:
       - Desktop: if showProfile, set active to true and go to "profile"
       - Load savedData: if exists, setData, applyTheme, go to "app"
       - If no savedData, go to "onboarding"
       - Web and desktop follow the same path (no branching)
       - Make the function non-async (no awaits needed)
    7. Remove the `handleWebGoogleSignIn` function entirely (lines 245-247)
    8. Replace `handleWebName` function (lines 249-257): remove `setSyncEnabled(true)` and `try { await pushData(fresh); } catch { /* offline */ }`. Keep only: create data, save to localStorage, set username, setData, setPhase("app"). Remove `async`.
    9. Replace `handleWebSignOut` function (lines 259-269): remove `await supabase.auth.signOut()`. Keep the localStorage clearing. Change `setPhase("google-signin")` to `setPhase("onboarding")`.
    10. Remove the `<Route path="/auth/callback" element={<AuthCallback />} />` from the Routes block (line 60)
    11. Remove the `syncConflict` state declaration: `const [syncConflict, setSyncConflict] = useState<AppData | null>(null);` (line 129)
    12. Remove the `resolveSyncConflict` function entirely (lines 343-355)
    13. Remove the entire sync conflict render block (the `if (syncConflict)` block, lines 359-385)
    14. Remove the entire `google-signin` phase render block (the `if (phase === "google-signin")` block, lines 395-413)
    15. Remove the entire `name-prompt` phase render block (the `if (phase === "name-prompt")` block, lines 415-431)
    16. Remove the `AuthCallback` function (the component defined at lines 746-781)
    17. Remove the `NameForm` function (the component defined at lines 704-719)
    18. Replace `handleDesktopContinue` (lines 290-317): remove `async`, remove all supabase.auth.getSession/pullData/setSyncEnabled/setSyncConflict calls. Keep only: load savedData, if exists setData, setStoredUsername, applyTheme, setPhase("app")
    19. In `handleSplashDone`, replace the web block (the `else` branch, lines 217-241): just use the same data-loading logic (load savedData, if exists go to app, else onboarding). No supabase/web-specific branching.

    IMPORTANT: Do NOT delete or modify any file in src/sync/ or src/supabase.ts. These files must remain on disk unchanged.
  </action>
  <verify>
    <automated>cd /home/margy/Documents/app-studio && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep -c "supabase.auth.getSession" src/App.tsx` returns 0
    - `grep -c "signInWithGoogle" src/App.tsx` returns 0
    - `grep -c "AuthCallback" src/App.tsx` returns 0
    - `grep -c "scheduleSync" src/DataContext.tsx` returns 0
    - `npx vite build` succeeds with no errors
    - Files src/sync/index.ts and src/supabase.ts are unchanged
  </done>

  <acceptance_criteria>
    - Grep `src/App.tsx` for `supabase.auth.getSession` returns 0 occurrences
    - Grep `src/App.tsx` for `signInWithGoogle` returns 0 occurrences
    - Grep `src/App.tsx` for `sb-lpnjxkzeyiifzzycmftg-auth-token` returns 0 occurrences
    - Grep `src/App.tsx` for `"google-signin"` returns 0 occurrences
    - Grep `src/App.tsx` for `"name-prompt"` returns 0 occurrences
    - Grep `src/App.tsx` for `AuthCallback` returns 0 occurrences
    - Grep `src/App.tsx` for `NameForm` returns 0 occurrences
    - Grep `src/App.tsx` for `syncConflict` returns 0 occurrences
    - Grep `src/App.tsx` for `resolveSyncConflict` returns 0 occurrences
    - Grep `src/App.tsx` for `from "./supabase"` returns 0 occurrences
    - Grep `src/App.tsx` for `from "./sync"` returns 0 occurrences
    - Grep `src/DataContext.tsx` for `scheduleSync` returns 0 occurrences
    - Grep `src/DataContext.tsx` for `pushData` returns 0 occurrences
    - Grep `src/DataContext.tsx` for `isSyncEnabled` returns 0 occurrences
    - Grep `src/DataContext.tsx` for `from "./sync"` returns 0 occurrences
    - `npx vite build` exits with code 0
    - `stat src/sync/index.ts` shows file still exists (code preserved)
    - `stat src/supabase.ts` shows file still exists (code preserved)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add data export/import buttons to Settings, remove auth and sync sections</name>
  <read_first>
    - /home/margy/Documents/app-studio/src/pages/Settings.tsx (the file being modified)
    - /home/margy/Documents/app-studio/src/storage/index.ts (reference for exportDataToFile and importDataFromFile signatures)
  </read_first>
  <files>src/pages/Settings.tsx</files>
  <action>
    **Import changes:**
    1. Remove `import { supabase } from "../supabase";` (line 1)
    2. Remove `import type { AppData } from "../storage/types";` (line 3) -- only used by sync conflict state
    3. Remove `import { saveData } from "../storage";` (line 5) -- only used in SyncSection
    4. Remove ALL sync imports (lines 6-16): remove the entire block from `import { isSyncEnabled, setSyncEnabled, ... } from "../sync";` 
    5. Add `exportDataToFile` and `importDataFromFile` to the storage import on line 5: change `import { saveData } from "../storage";` to `import { exportDataToFile, importDataFromFile } from "../storage";`

    **Component removals:**
    1. Remove the entire `WebAccountSection` function (lines 140-211) and its usage `{!isTauri && <WebAccountSection />}` (line 132)
    2. Remove the entire `SyncDeleteSection` function (lines 214-253)
    3. Remove the entire `SyncSection` function (lines 256-422) and its usage `{isTauri && <SyncSection />}` (line 135)
    4. Remove the `SYNC_RESOLVED_KEY` constant (line 255)

    **Add Data section:**
    After the Appearance card, before the closing `</div>` of the main settings container (which currently has `{!isTauri && <WebAccountSection />}` and `{isTauri && <SyncSection />}`), add a new "Data" card:

    ```
    {/* Data */}
    <div className="card bg-base-100 shadow-sm border border-base-300">
      <div className="card-body">
        <h3 className="font-semibold">Data</h3>
        <p className="text-sm text-base-content/60 mt-1">
          Export your data as a JSON file for backup, or import a previous backup.
        </p>
        <div className="flex gap-2 mt-3">
          <button className="btn btn-sm btn-primary" onClick={handleExport}>
            Export data
          </button>
          <button className="btn btn-sm btn-outline" onClick={handleImport}>
            Import data
          </button>
        </div>
        <p className="text-xs text-base-content/40 mt-2">
          Importing replaces all current data with the backup. This cannot be undone.
        </p>
      </div>
    </div>
    ```

    **Add handler functions:**
    Inside the `Settings` component (before the return statement), add two handler functions:

    ```
    const handleExport = async () => {
      await exportDataToFile(data);
    };

    const handleImport = async () => {
      const imported = await importDataFromFile();
      if (imported) {
        if (confirm("Importing will replace all current data. Continue?")) {
          setData(() => imported);
        }
      }
    };
    ```

    **Clean up unused code:**
    - Remove the `email` state variable (used only in WebAccountSection)
    - Remove unused React imports if Settings.tsx no longer uses `useEffect` (it was used in WebAccountSection's `useEffect` for session checking). The file will still need `useState` for `theme`, `accent`, `editingName`, `nameDraft` state variables so keep `useState`.
    - Keep the `deleteText`, `deleteConfirm` state variable... wait, those were in `WebAccountSection` which is now removed. Check if `confirming` and `text` state variables in `SyncDeleteSection` also need removal.

    After removing WebAccountSection and SyncDeleteSection, verify there are no references to:
    - `email`, `setEmail` (WebAccountSection)
    - `deleteConfirm`, `setDeleteConfirm`, `deleteText`, `setDeleteText` (WebAccountSection)
    - `confirming`, `setConfirming`, `text`, `setText` (SyncDeleteSection)
    - `syncOn`, `setSyncOn`, `lastSync`, `setLastSync`, `signedIn`, `setSignedIn`, `pushing`, `setPushing`, `conflict`, `setConflict` (SyncSection)
    - `pulling` (not used even in original code)
    
    If any of these remain as unused declarations (which would fail `noUnusedLocals`), remove them.

    Also remove `const ACCENTS` (still used by accent buttons, KEEP) and `THEME_KEY`, `ACCENT_KEY` (still used, KEEP).
  </action>
  <verify>
    <automated>cd /home/margy/Documents/app-studio && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep -c "exportDataToFile" src/pages/Settings.tsx` returns > 0
    - `grep -c "importDataFromFile" src/pages/Settings.tsx` returns > 0
    - `grep -c "SyncSection" src/pages/Settings.tsx` returns 0
    - `grep -c "WebAccountSection" src/pages/Settings.tsx` returns 0
    - `grep -c "signInWithGoogle" src/pages/Settings.tsx` returns 0
    - `npx vite build` succeeds with no errors
  </done>

  <acceptance_criteria>
    - Grep `src/pages/Settings.tsx` for `exportDataToFile` returns >= 1 occurrences
    - Grep `src/pages/Settings.tsx` for `importDataFromFile` returns >= 1 occurrences  
    - Grep `src/pages/Settings.tsx` for `SyncSection` returns 0 occurrences
    - Grep `src/pages/Settings.tsx` for `WebAccountSection` returns 0 occurrences
    - Grep `src/pages/Settings.tsx` for `signInWithGoogle` returns 0 occurrences
    - Grep `src/pages/Settings.tsx` for `deleteAccount` returns 0 occurrences
    - Grep `src/pages/Settings.tsx` for `from "../supabase"` returns 0 occurrences
    - Grep `src/pages/Settings.tsx` for `from "../sync"` returns 0 occurrences
    - Grep `src/pages/Settings.tsx` for `<span>Export data</span>` or `Export data` button text returns >= 1 occurrences (text varies, check for `Export data` in the file)
    - Grep `src/pages/Settings.tsx` for `<span>Import data</span>` or `Import data` button text returns >= 1 occurrences
    - `npx vite build` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix timer persistence on tray close (Dashboard.tsx)</name>
  <read_first>
    - /home/margy/Documents/app-studio/src/pages/Dashboard.tsx (the file being modified, lines 155-189 specifically)
  </read_first>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    Locate the `useEffect` that sets up `markClosed` and event listeners (currently lines 156-189 in Dashboard.tsx). The block to modify starts at `// ---- Mark timer for reset when tab/window is closed ----`.

    Changes:
    1. Remove the Tauri-specific block:
       ```
       // Tauri: listen for window close/destroy events (beforeunload doesn't fire on window hide)
       let tauriUnlisten: (() => void) | undefined;
       const isTauriEnv = typeof window !== "undefined" && ("__TAURI__" in window || "__TAURI_INTERNALS__" in window);
       if (isTauriEnv) {
         import("@tauri-apps/api/window").then(({ getCurrentWindow }) => {
           getCurrentWindow().onCloseRequested(async () => {
             markClosed();
           }).then((fn) => { tauriUnlisten = fn; });
         }).catch(() => {});
       }
       ```

    2. Remove the `tauriUnlisten` cleanup from the return function:
       Change:
       ```
       return () => {
         window.removeEventListener("beforeunload", markClosed);
         if (tauriUnlisten) tauriUnlisten();
       };
       ```
       To:
       ```
       return () => {
         window.removeEventListener("beforeunload", markClosed);
       };
       ```

    Keep the `beforeunload` listener and the `markClosed` function as-is -- these handle the web tab close case correctly.
    Keep the `loading` import and `debugSaveTimer` import -- they are used elsewhere in the file.

    After removing the Tauri block, verify that `isTauriEnv` is not referenced elsewhere in the file (it's only defined and used in this one block). If it was defined elsewhere, leave those references alone.
  </action>
  <verify>
    <automated>cd /home/margy/Documents/app-studio && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep -c "onCloseRequested" src/pages/Dashboard.tsx` returns 0
    - `grep -c "beforeunload" src/pages/Dashboard.tsx` returns > 0 (still present for web)
    - `npx vite build` succeeds with no errors
  </done>

  <acceptance_criteria>
    - Grep `src/pages/Dashboard.tsx` for `onCloseRequested` returns 0 occurrences
    - Grep `src/pages/Dashboard.tsx` for `tauriUnlisten` returns 0 occurrences
    - Grep `src/pages/Dashboard.tsx` for `isTauriEnv` returns 0 occurrences (only existed in that block)
    - Grep `src/pages/Dashboard.tsx` for `beforeunload` returns >= 1 occurrence (web handler preserved)
    - `npx vite build` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `npx vite build` -- must pass with no errors
2. Verify no unused imports or variables (tsconfig "noUnusedLocals": true, "noUnusedParameters": true)
3. Manual check: Settings page should show "Data" card with Export/Import buttons, no "Sync" or "Account" section
4. Manual check: App launches to splash -> onboarding on first run, no Google sign-in prompted
</verification>

<success_criteria>
- `npx vite build` passes cleanly
- grep confirms no onCloseRequested in Dashboard.tsx
- grep confirms exportDataToFile and importDataFromFile wired in Settings.tsx
- grep confirms no supabase.auth or sync function imports in App.tsx or DataContext.tsx
- src/sync/index.ts and src/supabase.ts remain on disk unchanged
</success_criteria>

<output>
After completion, verify build succeeds and summarize what was changed.
</output>
